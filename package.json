{
  "name": "ats",
  "version": "0.1.3",
  "description": "",
  "author": "G. Kay Lee <balancetraveller+github@gmail.com>",
  "scripts": {
    "setup": "npm cache clean && npm install",
    "update": "rm -rf node_modules && npm-check-updates -u && npm run setup",
    "deploy": "gulp build && ./deploy"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/gsklee/ats"
  },
  "keywords": [],
  "peerDependencies": {
    "babel": "^5.4.7"
  },
  "dependencies": {
    "bound-native-methods": "^0.1.4"
  },
  "devDependencies": {
    "gulp": "^3.9.0",
    "gulp-load-plugins": "^0.10.0",
    "gulp-babel": "^5.1.0"
  },
  "engines": {
    "iojs": ">=2.0.0"
  },
  "license": "MIT"
}
